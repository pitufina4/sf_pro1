{% extends 'base.html.twig' %}

{% block title %}Hello {{ controller_name }}!{% endblock %}

{% block body %}
<style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
</style>

<div class="example-wrapper">
    <h1>Hola {{ controller_name }}! ✅</h1>

    Este es mi primer controlador de  {{ minombre }}
    

	<ul>
	    {% for pizza in pizzas %}
	        <li>{{ pizza|capitalize }}</li>
	    {% endfor %}
	</ul>
	
	<ul>
	    {% for localidad in localidades %}
	        <li>{{ localidad|capitalize }}</li>
	    {% endfor %}
	</ul>

	<a href="{{ path('pizza_mostrar') }}"> Mostrar </a><br>
	<a href="{{ path('pizza_nuevo') }}"> Nuevo </a><br>
	<a href="{{ path('pizza_editar') }}"> Editar </a>

</div>

{% endblock %}
